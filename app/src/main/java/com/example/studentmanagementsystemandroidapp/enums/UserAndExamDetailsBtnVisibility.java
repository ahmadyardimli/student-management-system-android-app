package com.example.studentmanagementsystemandroidapp.enums;

public enum UserAndExamDetailsBtnVisibility {
    SHOW_BOTH, // Show both buttons
    HIDE_UPDATE, // Hide only the update button
    HIDE_DELETE, // Hide only the delete button
    HIDE_BOTH // Hide both buttons
}
